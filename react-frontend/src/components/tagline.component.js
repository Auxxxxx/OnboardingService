import React from 'react';

const Tagline = (props) =>{
    //  const propsList = props.list.map((item) =>
    //     (<li class = "innerList">{item}</li>)
    //     );


    return (
        <ul>
            {/* {propsList} */}
        </ul>
        // <div type = "text" className = "tagline">{props.text}</div>
    );

}

export default Tagline;